package ads.uninassau.brjobs.controller;

import ads.uninassau.brjobs.service.PrestadorService;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;

import static org.junit.jupiter.api.Assertions.assertNotNull;

@ExtendWith(MockitoExtension.class)
@DisplayName("PrestadorController Unit Tests")
class PrestadorControllerUnitTest {

    @Mock
    private PrestadorService prestadorService;

    @InjectMocks
    private PrestadorController prestadorController;

    @Test
    @DisplayName("Deve instanciar PrestadorController com sucesso")
    void testPrestadorControllerInstantiation() {
        // Assert
        assertNotNull(prestadorController);
    }

    // TODO: Métodos listar, obterPorId, criar, atualizar, desativar, ativar ainda não implementados no service
    // Estes testes necessitam ser reescritos quando os métodos do service forem implementados
}

